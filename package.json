{
  "name": "secondary-edits",
  "version": "0.1.0",
  "private": true,
  "description": "Training UI for secondary code edits in healthcare payment integrity.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "http-server -p 8080 -c-1 .",
    "dev": "npm run build && npm run serve",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "http-server": "^14.1.1"
  }
}
